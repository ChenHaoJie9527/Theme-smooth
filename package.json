{
  "name": "theme-smooth",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.4.8",
    "typescript": "^5.2.2"
  },
  "workspaces": [
    "packages/*"
  ],
  "license": "MIT"
}